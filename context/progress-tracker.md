# Progress Tracker

Update this file whenever the current phase, active feature, or implementation state changes.

## Current Phase
- Maintenance: Workspace Navbar Brand Link - COMPLETED

## Current Goal
- The canvas workspace navbar shows a centered white Ghost AI link that returns users to the editor home.

## Completed

- Maintenance: Workspace Navbar Brand Link - `components/editor/workspace-shell.tsx` now uses a three-column workspace navbar so the center displays a white/token-primary `Ghost AI` link. The link uses Next.js `Link` to return users from the canvas workspace to `/editor` while preserving the existing project title, sidebar toggle, Save, Templates, Share, and AI controls.

- Feature 29: Spec UI Integration - `components/editor/ai-sidebar.tsx` now replaces the placeholder Specs card with a compact scrollable list of generated specs for the active project, loading metadata from `GET /api/projects/[projectId]/specs` only while the sidebar is open. Each item shows the generated filename and `createdAt`, opens a shadcn/Base UI dialog preview on click, and provides a download action that targets the existing attachment endpoint so the browser handles the file. The preview dialog fetches Markdown through `GET /api/projects/[projectId]/specs/[specId]`, renders it with `react-markdown`, includes Close and Download actions, and clears preview content when closed instead of keeping spec content in long-lived state. Because the live checkout only had the Feature 28 download route, read-only ProjectSpec list and preview endpoints were added using the same Clerk auth, project access, and private Blob access pattern without exposing Blob URLs.

- Feature 28: Spec Persistence & Download - `ProjectSpec` now exists as Prisma metadata linked to `Project` with `id`, `projectId`, `filePath`, and `createdAt`, backed by migration `20260625000100_add_project_spec`, applied to the configured Postgres database, and the Prisma client has been regenerated. `trigger/generate-spec.ts` now uploads generated Markdown to private Vercel Blob storage under `specs/{projectId}/{timestamp}.md`, stores the Blob URL in `ProjectSpec.filePath`, links the record to the project, records `specId` in Trigger metadata, and returns `{ spec, specId }`. `GET /api/projects/[projectId]/specs/[specId]/download` authenticates the current Clerk user, verifies project access, verifies the spec belongs to the project, fetches the private Blob through the server, and returns it as a Markdown attachment without exposing the Blob URL. `npx prisma validate`, `npx prisma migrate deploy`, `npm run lint`, `npx tsc --noEmit`, `git diff --check`, and `npm run build` pass.

- Feature 27: Spec Generation Flow - `POST /api/ai/spec` now validates `roomId`, `chatHistory`, `nodes`, and `edges` with Zod, authenticates the current Clerk user, derives `projectId` from `roomId`, verifies project access without trusting a client-supplied project ID, triggers the `generate-spec` Trigger.dev task, stores a `TaskRun` ownership record, and returns the run ID. `POST /api/ai/spec/token` validates `runId`, verifies the run belongs to the authenticated user, and issues a Trigger.dev public read token scoped to that run with a one-hour expiration. `trigger/generate-spec.ts` exports the Zod-backed `generateSpec` schema task, uses Gemini through `@ai-sdk/google`, updates Trigger metadata with status/count/spec length/error details, and returns the generated Markdown as a plain string without persistence or frontend wiring. `npm run lint`, `npx tsc --noEmit`, and `npm run build` pass.

- Feature 26: Design Agent Frontend - `components/editor/ai-sidebar.tsx` now submits design prompts end to end from the AI Architect form: it publishes the user message to the shared `ai-chat` feed, calls `POST /api/ai/design` with the active `roomId` and `projectId`, obtains a public Trigger token through the existing token route when needed, stores the active run locally, and subscribes with `useRealtimeRun`. While a run is active, the textarea and send button are disabled, the send button shows a spinner, and a compact status strip reads the latest validated `ai-status-feed` message. Terminal run states publish a final Ghost AI assistant message back to `ai-chat`, errors are also surfaced as assistant feed messages, and canvas nodes/edges remain synced only through the existing Liveblocks `useLiveblocksFlow` path. `npm run lint` and `npx tsc --noEmit` pass.

- Feature 25: Sidebar Chat Feed - `types/tasks.ts` now defines the room-scoped `ai-chat` feed ID, a Zod-backed `ChatFeedMessageSchema`, and a parser that validates sender, role, content, and timestamp before rendering. `liveblocks.config.ts` types feed messages as either AI status payloads or chat payloads, keeping `ai-chat` separate from `ai-status-feed`. `components/editor/ai-sidebar.tsx` creates/reuses both feeds, subscribes to `ai-chat`, renders validated chat messages in timestamp order with sender and time metadata, styles current-user messages separately from collaborator messages, sends messages through the existing sidebar textarea and send button, clears the input only after a successful send, shows a small error state on failure, and no longer triggers backend AI work from this chat-only feature. `zod` is now a direct dependency because the feature schema imports it from app code. `npm run lint` and `npx tsc --noEmit` pass.

- Feature 24: AI Presence State - `types/tasks.ts` now defines the shared `ai-status-feed` ID, a JSON-compatible AI status feed payload schema, and validation helpers that accept optional `text` while preserving compatibility with existing `message` payloads. `liveblocks.config.ts` types `FeedMessageData` with that schema. The Liveblocks provider and room provider now wrap the active workspace area in `components/editor/workspace-shell.tsx`, giving both the canvas and `components/editor/ai-sidebar.tsx` direct room access while keeping the canvas behind its own `ClientSideSuspense` boundary in `components/editor/canvas/canvas-room.tsx`. The sidebar creates/reuses `ai-status-feed`, subscribes to the latest validated feed message, shows only that status in the compact banner, writes best-effort start/error status messages, disables the prompt and shows a loading send button while generation is active, and toggles `thinking` presence while the current request is active. `components/editor/canvas/presence-cursors.tsx` already reads `presence.thinking` and shows the spinner badge, matching the spec. `npm run lint` and `npx tsc --noEmit` pass.

- Maintenance: Hardened the Trigger design agent's Liveblocks room and presence path for local testing. `POST /api/ai/design` now prepares the Liveblocks room after project access is verified and returns a readable `502` if Liveblocks rejects room creation. `trigger/design-agent.ts` no longer creates rooms from the worker; it verifies the room exists, initializes missing `flow.nodes` / `flow.edges` storage, includes the full Ghost AI user metadata for server-set presence, and treats AI presence updates as best-effort with a warning instead of aborting the Trigger run. `eslint.config.mjs` and `.gitignore` now ignore generated `.trigger/tmp` files. `npm run lint`, `npx tsc --noEmit`, `git diff --check`, and `npm run build` pass.

- Maintenance: Added runtime payload validation to the Trigger design agent. `trigger/design-agent.ts` now uses `schemaTask` with a custom validator for non-empty `prompt` and `roomId` strings, trimming both values before execution and aborting invalid runs without retrying. This prevents direct Trigger tests with missing `roomId` from crashing later in Liveblocks with `Cannot read properties of undefined (reading 'trim')`. `npm run lint` and `npx tsc --noEmit` pass.

- Feature 23: Design Agent Logic - `trigger/design-agent.ts` now uses Gemini `gemini-2.0-flash` through `@ai-sdk/google` and AI SDK structured output to turn a user prompt plus the current Liveblocks canvas snapshot into a bounded set of canvas actions. The task supports add/move/resize/update/delete node actions and add/delete edge actions, normalizes generated shapes against `NODE_SHAPES`, colors against `NODE_COLORS`, dimensions against shape defaults, and positions onto a simple grid with spacing guardrails. Canvas updates are applied through the existing Liveblocks `flow` storage with `mutateStorage` and `LiveObject.from`, preserving the collaborative React Flow architecture. The task publishes start/processing/complete/error status with `broadcastEvent`, writes status messages to the shared `ai-status-feed` best-effort, sets Ghost AI presence with `thinking: true` while running, and clears that presence in `finally`. `liveblocks.config.ts` now types AI status room events and feed messages. `components/editor/canvas/canvas-editor.tsx`, `components/editor/canvas/canvas-room.tsx`, and `components/editor/workspace-shell.tsx` relay AI status events into `components/editor/ai-sidebar.tsx`, where the AI Architect form now calls `POST /api/ai/design`, disables while the run is active, and shows shared status. `components/editor/canvas/presence-cursors.tsx` shows a spinner when a participant, including Ghost AI, is thinking. `npm run lint`, `npx tsc --noEmit`, `git diff --check`, and `npm run build` pass.

- Feature 22: Design Agent API - `TaskRun` is now a Prisma model linked to `Project` with a unique `runId`, `projectId`, `userId`, `createdAt`, an index on `runId`, and a compound index on `userId` and `projectId`; migration `20260624115928_add_task_run` has been applied and the Prisma client regenerated. `trigger/design-agent.ts` exports the minimal `design-agent` task, accepts `prompt` and `roomId`, and logs/echoes the input without AI provider calls or canvas updates. `POST /api/ai/design` validates `prompt`, `roomId`, and `projectId`, verifies authenticated project access, triggers the task through Trigger.dev, stores the run in `TaskRun`, and returns the run ID. `POST /api/ai/design/token` validates the run ID, verifies ownership against `TaskRun`, creates a Trigger.dev public token scoped to that run, and returns it. `trigger.config.ts` now includes the SDK-required `maxDuration: 300`. `npm run lint`, `npx tsc --noEmit`, `npx prisma validate`, `git diff --check`, and `npm run build` pass.

- Maintenance: Fixed the local Trigger.dev CLI script on Windows. The outdated `@trigger.dev/cli` v3 dev dependency exposed only a `trigger-cli` binary, so `npm run trigger:dev` could not find `trigger`. The project now uses `trigger.dev` ^4.4.6, matching `@trigger.dev/sdk` ^4.4.6 and wiring the npm scripts to Trigger.dev v4 commands: `trigger dev` and `trigger deploy`. `npm run trigger:dev -- --help` and `npm ls @trigger.dev/sdk trigger.dev` pass.

- Maintenance: Added a workspace-only Save button to the workspace navbar. `hooks/use-canvas-autosave.ts` now exposes the same `saveNow` function used by debounced autosave, reports `saving` / `saved` / `error` transiently before returning to idle, and `components/editor/canvas/canvas-editor.tsx` threads those controls through `CanvasRoom` into `WorkspaceShell`. The navbar button appears only in the project workspace, not the editor home navbar, and manually triggers the canvas save path while showing Save, Saving..., Saved, or Error states.

- Maintenance: Fixed local canvas autosave showing a persistent Save error when `BLOB_READ_WRITE_TOKEN` is missing. The canvas save route now keeps production saves on Vercel Blob when the token exists, falls back to ignored `.local/canvas/{projectId}.json` snapshots for local development, stores the returned local or blob reference in `Project.canvasJsonPath`, and loads either storage form through the same GET endpoint. `.gitignore` now excludes `.local/`, and `context/architecture-context.md` documents the local development fallback. `npm run lint`, `npx tsc --noEmit`, `npx prisma validate`, `git diff --check`, and `npm run build` pass.

- Maintenance: Fixed Vercel Blob autosave for the configured private Blob store. The canvas route now uploads canvas snapshots with `access: "private"` and reads saved snapshots back through `@vercel/blob` `get(..., { access: "private", useCache: false })` instead of unauthenticated fetch, while preserving the local fallback for missing Blob tokens.

- Feature 21: Canvas Autosave - `@vercel/blob` is installed. The existing Prisma `Project.canvasJsonPath` metadata field is reused as the canvas blob URL reference, keeping Prisma responsible for metadata while Vercel Blob stores the canvas JSON. `app/api/projects/[projectId]/canvas/route.ts` adds authenticated and project-access-checked GET/PUT handlers; PUT validates a `{ nodes, edges }` canvas snapshot, uploads it to `canvas/{projectId}.json` in Vercel Blob without a random suffix, allows overwrite for autosave, and stores the returned URL on the project record; GET reads that URL from Prisma, fetches the saved JSON, validates the snapshot shape, and returns an empty canvas when nothing has been saved. `hooks/use-canvas-autosave.ts` watches Liveblocks-backed nodes and edges, skips the initial render, debounces saves for 2 seconds, writes through the canvas API, and tracks idle/saving/saved/error status. `components/editor/canvas/canvas-editor.tsx` loads saved canvas state only once and only while the Liveblocks room is still empty, skips loading when active nodes or edges already exist, fits the restored view, wires autosave, and renders a small editor Save status button for saving/saved/error states. `components/editor/canvas/canvas-room.tsx` and `components/editor/workspace-shell.tsx` thread the project ID through without changing unrelated canvas behavior. `npm run lint`, `npx tsc --noEmit`, `npx prisma validate`, `git diff --check`, and `npm run build` pass.

- Feature 20: AI Sidebar Shell - `components/editor/ai-sidebar.tsx` now owns the floating AI sidebar UI while keeping open/close state controlled by `components/editor/workspace-shell.tsx`. The sidebar preserves the existing right-side floating slide-in placement, border, backdrop blur, and shadow treatment; adds an `AI Workspace` header with bot icon, subtitle, and close button; and uses the existing project tokens for dark surfaces, muted copy, brand accents, and AI accents. The AI Architect tab includes a scrollable empty state, three starter prompt pills, local user message rendering, an auto-resizing 72px-160px textarea, Enter-to-send behavior with Shift+Enter newlines, and a token-styled send button. The Specs tab includes a Generate Spec button and a static demo spec card with file icon, snippet, and disabled Download action. No backend, Liveblocks, or AI generation logic was added. `npm run lint`, `npx tsc --noEmit`, `git diff --check`, and `npm run build` pass.

- Feature 19: Presence Avatars & Live Cursors - `liveblocks.config.ts` now defines shared presence with `cursor` and `thinking`, and `components/editor/canvas/canvas-room.tsx` initializes both fields. `components/editor/canvas/collaborator-avatars.tsx` renders a canvas-scoped top-right participant group using `useOthers` plus Clerk's active `useUser`, filters out the current Clerk user, shows up to five display-only collaborator avatars with photo/initial fallbacks and a +N overflow chip, and renders the Clerk `UserButton` separately with a divider only when collaborators exist. `components/editor/canvas/presence-cursors.tsx` renders other participants' cursor pointers and name badges using their Liveblocks color, while `components/editor/canvas/canvas-editor.tsx` broadcasts cursor position from React Flow mouse movement via `useUpdateMyPresence`, clears it on mouse leave, and leaves node/edge behavior unchanged. The editor home navbar is unchanged, and the workspace-level Clerk button was moved into the canvas presence group so the current user is not duplicated in the room UI. `npm run lint`, `npx tsc --noEmit`, and `npm run build` pass.

- Feature 18: Starter Templates — `components/editor/starter-templates.ts` defines a typed starter template library with Microservices Architecture, CI/CD Pipeline, and Event-Driven System templates using shared canvas node/edge types, the existing `NODE_COLORS` palette, and shape defaults. `components/editor/starter-templates-modal.tsx` renders a wide import dialog with a scrollable responsive card grid, enlarged template cards, larger fixed-viewport SVG/CSS previews fitted from template node bounds, descriptions, and import buttons. `components/editor/workspace-shell.tsx` adds a workspace navbar Templates button and owns modal/pending-template state. `components/editor/canvas/canvas-room.tsx` threads pending imports into `components/editor/canvas/canvas-editor.tsx`, where the selected template clears existing collaborative nodes and edges through the Liveblocks-backed React Flow change handlers, adds cloned template nodes and edges, and fits the view after import. No server persistence, custom template saving, or node/edge renderer behavior changes were added. `npm run lint`, `npx tsc --noEmit`, and `npm run build` pass.

- Feature 17: Canvas Ergonomics — removed the React Flow MiniMap and added `components/editor/canvas/canvas-controls.tsx`, a bottom-left floating pill with animated zoom out, fit view, and zoom in actions plus Liveblocks-backed undo and redo controls separated by a thin divider. Undo and redo buttons reflect `useCanUndo` / `useCanRedo` and dim when disabled. `hooks/useKeyboardShortcuts.ts` receives the React Flow instance and history handlers, supports `+` / `=` zoom in, `-` zoom out, Cmd/Ctrl+Z undo, Cmd/Ctrl+Shift+Z redo, and Cmd/Ctrl+Y redo, and ignores inputs, textareas, and contenteditable fields. The shape panel, node and edge renderers, and collaborative state setup are unchanged. `npm run lint`, `npx tsc --noEmit`, `git diff --check`, and `npm run build` pass. Interactive browser verification was unavailable because the in-app browser connection could not be established.

- Feature 16: Edge Behavior — the existing top/right/bottom/left node handles remain subtle hover-revealed source handles that can start or receive loose-mode connections. New connections are added through the Liveblocks-backed `onEdgesChange` flow as typed `canvasEdge` records with rounded smooth-step previews and closed arrow markers. `components/editor/canvas/canvas-edge.tsx` renders right-angle routes from `getSmoothStepPath`, dims idle edges, brightens hovered or selected edges, and adds a transparent 20px interaction path without thickening the visible stroke. Labels use `EdgeLabelRenderer` at the returned path midpoint; double-clicking the edge or badge opens a growing input, blur/Enter/Escape saves through a Liveblocks mutation, saved labels render as pill badges, and selected unlabeled edges show a faint hint. `npm run lint`, `npx tsc --noEmit`, `git diff --check`, and `npm run build` pass. Interactive verification was unavailable because the in-app browser connection failed before opening the local app.

- Feature 15: Node Color Toolbar — `CanvasNodeData` now carries an optional collaborative `textColor` alongside its background `color`, with new nodes initialized from the default pair in `NODE_COLORS`. Selected nodes render a compact `NodeToolbar` above the node with all eight predefined palette pairs; each 20px swatch prevents node dragging and canvas panning, shows its paired text color as the active border, and uses that color for a tight hover glow. Selecting a swatch updates both `color` and `textColor` in the existing Liveblocks flow storage in one mutation, so the node surface, label, empty-label hint, and inline editor update immediately without a server call. Drag/drop, selection, resizing, shape rendering, and connection behavior are unchanged. `npm run lint`, `npx tsc --noEmit`, `git diff --check`, and `npm run build` pass. Interactive browser verification was unavailable because the in-app browser connection could not be established.

- Feature 14: Node Editing — `components/editor/canvas/canvas-node.tsx` shows subtle dark-canvas `NodeResizer` handles only for selected nodes, enforces a 60×40 minimum size, and sends dimension changes through the existing React Flow `onNodesChange` handler supplied by `useLiveblocksFlow`. The centered label layer opens a directly overlaid textarea on double-click, preserves its layout while editing, shows centered placeholder text for empty labels, writes each change to the existing Liveblocks node data, closes on blur or Escape, and prevents textarea interactions from dragging nodes or panning the canvas. Shape rendering, the shape panel, drag previews, and dropped-node creation are unchanged. `npm run lint`, `npx tsc --noEmit`, `git diff --check`, and `npm run build` pass. Interactive browser inspection was unavailable in the session.

- Feature 13: Node Shape Rendering — `components/editor/canvas/node-shape-surface.tsx` provides one shared renderer for collaborative nodes and drag previews: rectangle, pill, and circle use CSS surfaces, while diamond, hexagon, and cylinder use scalable inline SVG geometry with subtle idle borders and brighter selected borders. `components/editor/canvas/canvas-node.tsx` uses that renderer without changing its existing Liveblocks-backed label or connection behavior. `components/editor/canvas/shape-panel.tsx` suppresses the native browser drag image and renders a pointer-events-free fixed ghost at the cursor using the dragged shape's existing default width and height, updating throughout the drag and clearing on drop or cancellation. Existing panel layout, drag payloads, drop creation, and collaborative storage writes are unchanged. `npm run lint`, `npx tsc --noEmit`, `git diff --check`, and `npm run build` pass. Live browser inspection was unavailable in the session.

- Maintenance: Removed canvas node resizing at user request while preserving drag, labels, and shape rendering. Repaired four-side click-to-connect by moving the top/right/bottom/left handles above the node label layer, making the non-editing label layer click-through, keeping the textarea interactive during editing, and explicitly enabling React Flow click connections with a larger connection radius. Users can now click one side handle and then click any side handle on another node to connect them.

- Maintenance: Corrected the canvas node interaction issues shown in the latest screenshot. `components/editor/canvas/canvas-node.tsx` now renders rectangle, pill, and circle as distinct CSS shapes and diamond, hexagon, and cylinder as scalable SVG geometry; every node has named top, right, bottom, and left handles that can start or receive loose-mode connections; and double-clicking a node opens a centered collaborative label editor with a visible muted placeholder, white entered text, and Escape/blur dismissal.

- Maintenance: Fixed the editor canvas issues documented in `context/current-issues.md`. The canvas now fills the workspace edge-to-edge without card borders, radius, padding, or elevated background; the React Flow dotted background remains visible beneath absolutely positioned project and AI sidebars; both sidebars transition completely outside the workspace when closed; and shape drag-and-drop now opts out of React Flow pan gestures, supplies custom and plain-text payloads, handles drops on the React Flow root, converts screen coordinates through `screenToFlowPosition`, centers nodes at the drop point, and persists them to Liveblocks storage.

- Feature 07: Wire Editor Home — PROPERLY IMPLEMENTED (was marked complete but not actually implemented). app/editor/page.tsx converted to server component using async/await to fetch owned and shared projects via lib/projects.ts getProjectsForUser(). EditorHomeClient (components/editor/editor-home-client.tsx) is the client shell receiving real ProjectRow arrays. lib/project-access.ts provides getCurrentUserIdentity() and userHasProjectAccess() helpers. hooks/use-project-actions.ts replaces the removed mock hook: create generates one stable cryptographically random 6-character suffix per dialog opening, combines it with the live slug preview, POSTs to /api/projects with the matching project/room id, and navigates to /editor/[roomId]; rename PATCHes and router.refresh(); delete DELETEs and redirects to /editor if the target is the active workspace, otherwise router.refresh(). ProjectSidebar links project items to /editor/[id]. ProjectDialogs is wired on both editor home and workspace shells, so active-workspace deletion is reachable. Create dialog shows live room ID preview. Rename dialog pre-fills current name. Delete dialog shows project name. All routes and hooks type ProjectRow consistently. npm run build passes clean.

- Feature 06: Project APIs — GET /api/projects lists owned projects for authenticated users (ownerId lookup); POST /api/projects creates projects with optional custom id field and default "Untitled Project" name; PATCH /api/projects/[projectId] renames projects (owner-only); DELETE /api/projects/[projectId] deletes projects (owner-only). All routes enforce Clerk authentication and project ownership checks, reject malformed or non-object JSON with 400 responses, and return 401 for unauthenticated requests. TypeScript, production build, route compilation, and live unauthenticated endpoint checks pass.

- Maintenance: Clarified the Clerk setup skill's manual Dashboard fallback with framework-specific publishable and secret key environment variable names for Next.js, Vite-based React/Vue, Astro, Nuxt, React Router, TanStack Start, Express, Fastify, and Expo.

- Maintenance: Corrected the Clerk Core 3 custom sign-up example to destructure `error` from `signUp.create()`, allowing phone-code verification preparation to run only when account creation succeeds.

- Maintenance: Updated the Clerk Core 3 custom sign-in example to handle `needs_client_trust`, select an available second-factor strategy from `supportedSecondFactors`, prepare phone, email-code, and email-link factors with the matching API, and verify phone, email, TOTP, or backup codes using the tracked strategy.

- Maintenance: Added explicit `text` language tags to all previously unlabeled fenced code blocks in the Clerk Backend API skill documentation, satisfying markdownlint MD040 without changing example content.

- Maintenance: Replaced the Clerk Backend API skill's secret-key preflight output with an unset-safe, non-disclosing pass/fail check so credential material is never printed.

- Maintenance: Hardened the Clerk Backend API request helper so curl fails on HTTP 4xx/5xx responses, limits connection setup to 10 seconds, and caps total request time at 60 seconds.

- Sign-in card polish: Added a light `--text-primary` outline to the rounded black sign-in card and removed its shadow. The two OAuth buttons remain side by side with GitHub left and Google right, while their visible labels are now exactly "GitHub" and "Google" instead of Clerk's longer "Continue with" text. Only the sign-in component was changed. ESLint, TypeScript, production build, and rendered appearance-prop checks pass.

- Sign-in-only Clerk refinement: The sign-in card and footer now use the base dark background instead of the lighter surface color; GitHub and Google render side by side with GitHub ordered left and Google right; and the "Don't have an account?" footer action has a visible token-based bottom border separating it from the secured-by-Clerk area. The overrides live only on the `/sign-in` `<SignIn />` component, leaving sign-up and shared auth components unchanged. ESLint, TypeScript, production build, and rendered appearance-prop checks pass.

- Auth UI refinement: Updated the shared sign-in/sign-up shell to a true large-screen 50/50 layout inspired by the approved reference. The left half now uses the elevated surface token, cyan brand rail and logo, compact product statement, icon-led feature list, and restrained footer; the right half keeps the Clerk form centered on the base background. Geist Sans is explicitly applied to the document and Clerk text/buttons, with Geist Mono assigned to Clerk mono content. No gradients, hardcoded colors, or Clerk flow rewrites were introduced. ESLint, TypeScript, production build, and live `/sign-in` and `/sign-up` markup checks pass.

- Clerk CLI authentication setup: Clerk CLI 1.5.0 installed and authenticated, project linked to the Ghost AI Clerk app `app_3FUUR6Snn1mzSHXtv05ehTKmuz4`, and its development keys pulled into the ignored `.env.local`. `@clerk/nextjs` and `@clerk/ui` installed; ClerkProvider uses Clerk's dark theme with CSS-variable appearance overrides inside `<body>`; sign-in and sign-up are the only public app routes and are resolved from the existing Clerk env vars; `/` and `/editor` redirect unauthenticated users to sign-in; EditorNavbar shows UserButton; and the proxy matcher includes `/__clerk/:path*` once after the API/TRPC matcher. Clerk doctor, ESLint, TypeScript, production build, and runtime route behavior verified.

- Maintenance: Corrected Tabs orientation Tailwind variants to target Base UI's `data-orientation="horizontal|vertical"` attributes, restoring orientation-specific layout and indicator styles.

- Maintenance: Forwarded the Tabs wrapper's orientation prop to Base UI's Tabs Root so vertical tabs use the primitive's intended keyboard navigation behavior.

- Maintenance: Corrected the ScrollArea scrollbar Tailwind data-attribute variants to target Base UI's `data-orientation="horizontal|vertical"` values, restoring orientation-specific sizing and border styles.

- Feature 01: Design System — shadcn/ui installed and configured for Tailwind v4 with the Base Nova style, dark-only project and semantic theme tokens defined in globals.css, Button/Card/Dialog/Input/Tabs/Textarea/ScrollArea generated in components/ui/, lucide-react installed, and lib/utils.ts provides the reusable cn() helper. ESLint, TypeScript, production build, class merging, and emitted dark-theme CSS verified.
- Feature 02: Editor Chrome — EditorNavbar (fixed-height top bar with left/center/right sections and PanelLeftOpen/PanelLeftClose toggle) and ProjectSidebar (fixed overlay that slides from the left without pushing the canvas, Projects header, My Projects/Shared tabs with empty states, and full-width New Project button) added to components/editor/. A minimal `/editor` route composes both through EditorShell so the chrome works end to end. Dialog title, description, and footer action primitives are ready via the existing token-based components/ui/dialog.tsx. TypeScript, ESLint, and production build clean.
- Feature 03: Auth — @clerk/ui installed. ClerkProvider wraps root layout with dark theme from @clerk/ui/themes, overriding appearance variables using CSS tokens (no hardcoded colors). proxy.ts at project root uses clerkMiddleware + createRouteMatcher to protect all routes except /sign-in and /sign-up (resolved from NEXT_PUBLIC_CLERK_SIGN_IN_URL / NEXT_PUBLIC_CLERK_SIGN_UP_URL env vars). Sign-in and sign-up pages use a minimal two-panel layout (left panel with logo/tagline/feature list hidden on mobile, right panel with centered Clerk form). app/page.tsx redirects authenticated users to /editor and unauthenticated users to /sign-in. UserButton added to EditorNavbar right section. app/editor/page.tsx shell created with sidebar state management.
- Feature 04: Project Dialogs — hooks/use-project-dialogs.ts manages dialog/form/loading state and mock project data (CRUD operations on local state). components/editor/project-dialogs.tsx renders Create (name + live slug preview), Rename (prefilled, auto-focus, Enter submits), and Delete (destructive confirm) dialogs. ProjectSidebar now lists owned and shared mock projects, shows rename/delete actions only for owned projects, wires the sidebar create action, and adds a mobile backdrop scrim that closes the sidebar. EditorShell renders the centered `/editor` home heading, description, and New Project button wired to the Create dialog. ESLint, TypeScript, production build, emitted `/editor` content, and protected-route redirect behavior verified.
- Feature 05: Prisma Setup — prisma/models/project.prisma defines Project and ProjectCollaborator with the specified status enum, relations, cascade behavior, timestamps, uniqueness, and indexes. lib/prisma.ts exports a development-cached singleton that uses Accelerate for prisma+postgres URLs and @prisma/adapter-pg otherwise. Migration 20260622151436_init applied to the hosted PostgreSQL development database and the Prisma client generated to lib/generated/prisma. Prisma validation, migration status, direct client query, ESLint, TypeScript, and production build pass.
- Feature 06: Project APIs — GET /api/projects lists owned projects for authenticated users (ownerId lookup); POST /api/projects creates projects with optional custom id field and default "Untitled Project" name; PATCH /api/projects/[projectId] renames projects (owner-only); DELETE /api/projects/[projectId] deletes projects (owner-only). All routes enforce Clerk authentication and project ownership checks. TypeScript, production build, and route compilation pass.
- Feature 07: Wire Editor Home — app/editor/page.tsx converted to server component; fetches owned projects (by ownerId) and shared projects (via ProjectCollaborator email lookup) using lib/projects.ts getProjectsForUser(). EditorHomeClient (components/editor/editor-home-client.tsx) is the new client shell receiving serialized ProjectRow arrays. hooks/use-project-actions.ts replaces mock hook: create generates roomId (slugify+suffix), POSTs to /api/projects with custom id field, navigates to /editor/[id]; rename PATCHes and router.refresh(); delete DELETEs and redirects to /editor if active workspace else refresh. POST API updated to accept optional id. ProjectSidebar updated to accept ownedProjects/sharedProjects separately; project items link to /editor/[id]. ProjectDialogs updated to use useProjectActions type, slug renamed to roomId. Build clean.
- Feature 08: Editor Workspace Shell — app/editor/[roomId]/page.tsx is a server component using Next.js 16 async params; unauthenticated users redirect to /sign-in, while missing and unauthorized projects render AccessDenied. lib/project-access.ts returns the current Clerk user ID plus normalized primary email and checks access by project owner ID or collaborator email. components/editor/workspace-shell.tsx renders the full-viewport project workspace with project-aware navbar actions, the existing ProjectSidebar, centered canvas placeholder, and toggleable right AI placeholder. ProjectSidebar highlights the current project and opens the Shared tab when the active room is shared. `npm run lint`, `npx tsc --noEmit`, `npm run build`, and a live unauthenticated 307 redirect check all pass.
- Feature 09: Share Dialog — app/api/projects/[projectId]/collaborators/route.ts now lists collaborators for owners and collaborators, while POST/DELETE enforce owner-only invite and removal server-side. lib/project-collaborators.ts normalizes stored emails and enriches matches with Clerk display names and avatars, falling back to email-only rows. components/editor/project-share-dialog.tsx and hooks/use-project-share.ts provide owner invite/remove/copy-link controls with temporary `Copied!` feedback and a read-only collaborator state; components/editor/workspace-shell.tsx opens the dialog from the workspace Share button. `npm run lint`, `npx tsc --noEmit`, `npm run build`, unauthenticated workspace redirect, and collaborator API 401 behavior all pass.
- Feature 10: Liveblocks Setup — `liveblocks.config.ts` defines Presence with cursor coordinates and `isThinking`, plus UserMeta with user ID, display name, avatar URL, and cursor color. `lib/liveblocks.ts` provides a lazily initialized cached Liveblocks Node client and deterministic user-color helper. `POST /api/liveblocks-auth` requires Clerk authentication, validates the requested project room ID, verifies access with `userHasProjectAccess`, creates a private room only when needed, and returns a full-access session token carrying Clerk user metadata. Added the missing `@liveblocks/node` dependency. `npm run lint`, `npx tsc --noEmit`, `git diff --check`, and `npm run build` pass; the production build includes `/api/liveblocks-auth`.
- Workspace shell visual refinement — components/editor/workspace-shell.tsx now matches the supplied dark three-panel reference with a compact project-aware navbar, labeled Share/AI actions, user avatar, floating rounded project/canvas/AI panels, grid-and-glow canvas placeholder, and structured AI placeholder. components/editor/project-sidebar.tsx adds a workspace-only presentation variant so the editor home sidebar remains unchanged. Existing Share, AI toggle, project CRUD, routing, and API behavior are preserved. `npm run lint`, `npx tsc --noEmit`, and `npm run build` pass.
- Feature 11: Base Canvas — `types/canvas.ts` defines typed `canvasNode` and `canvasEdge` contracts with label, color, and shape node data. `liveblocks.config.ts` types cursor presence and the React Flow-backed `flow` storage used by the installed `@liveblocks/react-flow` package. `components/editor/canvas/canvas-room.tsx` sets up `LiveblocksProvider`, `RoomProvider`, empty room storage, `ClientSideSuspense`, and a connection error fallback. `components/editor/canvas/canvas-editor.tsx` renders Liveblocks-synced nodes and edges with loose connections, `fitView`, `MiniMap`, and a dot background. The server workspace page remains server-side, and `WorkspaceShell` replaces its placeholder with `CanvasRoom`. `npm run lint`, `npx tsc --noEmit`, and `npm run build` pass.
- Feature 12: Shape Panel — `types/canvas.ts` defines the six supported shapes, eight node color pairs, and sensible per-shape default dimensions. `components/editor/canvas/shape-panel.tsx` adds a bottom-center floating pill with six draggable icon buttons whose JSON payload includes shape, width, and height. `components/editor/canvas/canvas-node.tsx` provides the basic bordered custom node renderer with centered labels and four handles. `CanvasRoom` supplies `ReactFlowProvider`, while `CanvasEditor` validates dropped payloads, converts screen coordinates with `screenToFlowPosition`, generates shape/timestamp/counter IDs, and writes empty-label default-color `canvasNode` records into Liveblocks storage. `npm run lint`, `npx tsc --noEmit`, `git diff --check`, and `npm run build` pass.

## Unverified Legacy Notes

The Feature 17+ entries below were already present in this tracker, but their described files and dependencies are not present in the live checkout. They are retained as unverified historical notes and must not be treated as completed implementation.
- Feature 17: Canvas Ergonomics — MiniMap removed. components/editor/canvas/canvas-controls.tsx added: pill-shaped floating bar at bottom-left with zoom out/fit view/zoom in (via useReactFlow), a thin divider, and undo/redo (via useUndo/useRedo/useCanUndo/useCanRedo from @liveblocks/react); disabled buttons are dimmed (opacity-30). hooks/useKeyboardShortcuts.ts created: receives ReactFlow instance + undo/redo handlers, listens on window keydown, skips INPUT/TEXTAREA/contentEditable targets; supports +/= zoom in, - zoom out, Cmd/Ctrl+Z undo, Cmd/Ctrl+Shift+Z redo, Cmd/Ctrl+Y redo. `npm run build` passes clean.
- Feature 19: Presence Avatars & Live Cursors — liveblocks.config.ts Presence type updated (isThinking → thinking). canvas-room.tsx initialPresence updated to match. components/editor/canvas/collaborator-avatars.tsx renders a top-right overlay inside the canvas: up to 5 collaborator avatars (photo or initials fallback, ring, overlapping stack, +N overflow chip) from useOthers filtered to exclude the current Clerk user, a divider shown only when collaborators exist, and the Clerk UserButton. components/editor/canvas/presence-cursors.tsx renders live cursors for all other participants using useOthers + useReactFlow's flowToScreenPosition + useViewport (for pan/zoom reactivity); each cursor is a colored SVG pointer with a name badge. canvas-editor.tsx wires useMyPresence to broadcast cursor position on onMouseMove (screenToFlowPosition) and clear on onMouseLeave; renders PresenceCursors and CollaboratorAvatars inside the canvas wrapper. Editor home navbar is unchanged. `npm run build` passes clean.
- Feature 22 (Trigger.dev Integration): @trigger.dev/sdk ^4.4.4 installed. trigger.config.ts at project root: project ref from TRIGGER_PROJECT_REF env var, dirs ["./trigger"], maxDuration 300s, 3-attempt retry with backoff. trigger/generate-design.ts: durable task skeleton (id "generate-design") accepting projectId/roomId/prompt/userId payload — TODOs for Claude API call and Liveblocks storage writes. trigger/generate-spec.ts: durable task skeleton (id "generate-spec") accepting projectId/specId/userId/canvasJson — TODOs for Claude API call, Vercel Blob upload, and Prisma spec record update. No webhook route handler needed (v3+ executes on Trigger.dev cloud workers; tasks are triggered via task.trigger() with TRIGGER_SECRET_KEY). `npm run build` passes clean.
- Feature 22 (Design Agent API): TaskRun Prisma model (runId unique, projectId, userId, createdAt; indexes on runId and userId+projectId) added and migrated. trigger/design-agent.ts: minimal task (id "design-agent") accepting prompt and roomId, logs and echoes input. app/api/ai/design/route.ts: POST accepts prompt/roomId/projectId, triggers design-agent via tasks.trigger(), creates TaskRun record, returns runId. app/api/ai/design/token/route.ts: POST accepts runId, verifies ownership against TaskRun, generates Trigger.dev public token scoped to that run. `npm run build` passes clean.
- Feature 24 (AI Presence State): LiveblocksProvider + RoomProvider lifted to editor-workspace-client.tsx so sidebar has direct room access. AiSidebar uses useFeedMessages("ai-status-feed") for shared status banner, useCreateFeedMessage to write status on send, useUpdateMyPresence to toggle thinking, and useEventListener directly for broadcast-event-driven message thread. AiStatusFeedMessageSchema defined in types/tasks.ts; feed messages validated before display. PresenceCursors shows Loader2 spinner in badge when other.presence.thinking is true. `npm run build` passes.
- Feature 23 (Design Agent Logic): trigger/design-agent.ts fully implemented: uses Gemini (gemini-2.0-flash via @ai-sdk/google createGoogleGenerativeAI + generateText with Output.object) to generate structured canvas actions (add/move/resize/update/delete node + add/delete edge) from user prompt; uses Liveblocks Node SDK setPresence to show AI thinking state to all collaborators; uses broadcastEvent to push status messages at start/thinking/complete/error; uses mutateStorage with LiveObject.from to apply changes through existing collaborative storage; clears AI presence in finally block. liveblocks.config.ts: RoomEvent typed as ai-status events. canvas-editor.tsx: useEventListener bubbles broadcastEvent status to onAiStatus callback prop. canvas-room.tsx + editor-workspace-client.tsx: thread onAiStatus callback down from workspace to canvas. ai-sidebar.tsx: calls POST /api/ai/design on send; streams AI status into the message thread via aiStatusMessage prop; shows Loader2 spinner while task runs; disables input during generation. `npm run build` passes clean.
- Feature 25 (Sidebar Chat Feed): ChatFeedMessageSchema (sender, role, content, timestamp) added to types/tasks.ts. liveblocks.config.ts FeedMessageData extended with chat fields (sender, role, content, timestamp) alongside existing ai-status-feed fields. ai-sidebar.tsx: CHAT_FEED_ID "ai-chat" feed created on mount alongside "ai-status-feed"; useFeedMessages("ai-chat") subscribes to chat messages; feed messages validated with ChatFeedMessageSchema before rendering; Chat tab added between AI Architect and Specs tabs; messages rendered in order with sender name, formatted timestamp, and content; current user's messages right-aligned (accent-primary style), others left-aligned (bg-elevated style); input clears on successful send; error banner shown on failure; auto-scroll to bottom on new messages. `npm run build` passes clean.
- Feature 26 (Design Agent Frontend): ai-sidebar.tsx wired end-to-end: submit pushes user message to ai-chat feed and calls POST /api/ai/design → reads runId, then POST /api/ai/design/token → reads publicToken; useRealtimeRun(runId, { accessToken: publicToken }) tracks run in real time; terminal run status (COMPLETED/FAILED/etc.) pushes final AI message to ai-chat feed and resets loading state; useEventListener updates local statusText for mid-run status display; compact status strip shown above input only while isLoading; AI Architect tab renders validatedChatMessages from ai-chat instead of local state; user bubbles use #62C073 green; AI bubbles use dark bg + accent-ai-text; Chat tab also uses validatedChatMessages with same styling; types/tasks.ts ChatFeedMessageSchema role updated to enum(["user","assistant"]); liveblocks.config.ts FeedMessageData role updated to "user" | "assistant". `npm run build` passes clean.
- Feature 27 (Spec Generation Flow): app/api/ai/spec/route.ts — POST accepts roomId/chatHistory/nodes/edges, authenticates via Clerk, resolves projectId from roomId using getAccessibleProject (no client-supplied projectId), triggers generate-spec task, creates TaskRun record, returns runId. app/api/ai/spec/token/route.ts — POST accepts runId, verifies TaskRun ownership, issues Trigger.dev public token scoped to that run with 1-hour expiry. trigger/generate-spec.ts — schemaTask (id "generate-spec") with Zod-validated payload (projectId/roomId/chatHistory/nodes/edges); uses Gemini gemini-2.0-flash via @ai-sdk/google generateText to produce a structured Markdown spec from canvas context and chat history; tracks status/specLength in run metadata; returns { spec } as plain Markdown string. `npm run build` passes clean.
- Feature 29 (Spec UI Integration): GET /api/projects/[projectId]/specs lists specs for a project; GET /api/projects/[projectId]/specs/[specId] returns spec content as text/markdown for preview (not as attachment). ai-sidebar.tsx Specs tab: fetches spec list when sidebar opens, renders clickable compact list with filename/createdAt, download button per item; preview Dialog (base-ui) shows spec content rendered via react-markdown with dark-theme styling; download action creates a temporary anchor to the download endpoint and lets the browser handle the file. react-markdown ^10.1.0 installed. Build clean.

## In Progress

None.

## Next Up

- None.




## Open Questions

- None yet.

## Architecture Decisions

- shadcn/ui over Tailwind v4 (CSS-based token config via @theme inline in globals.css, no tailwind.config.js).
- Dark-only theme: all shadcn :root variables set to dark values directly — no .dark class switching.
- Do not modify generated components/ui/* files after shadcn installation.
- Next.js 16 uses proxy.ts (not middleware.ts) — same API, renamed to reflect its purpose.

## Session Notes

- Using Next.js 16.2.9 with React 19 and Tailwind CSS v4.
- shadcn version 4.5.0 was used; it auto-detected Tailwind v4.
- lucide-react ^1.11.0 installed as a direct dependency.
- @clerk/nextjs ^7.5.7 and @clerk/ui ^1.20.0 installed.
- @liveblocks/node installed alongside existing @liveblocks/client, @liveblocks/react, @liveblocks/react-flow, @liveblocks/react-ui. Liveblocks client uses lazy init (getLiveblocks()) to avoid key validation errors at build time.
- @vercel/blob ^2.4.1 installed. BLOB_READ_WRITE_TOKEN is not currently set in .env.local, so local development canvas autosave falls back to ignored `.local/canvas` snapshot files until a real Vercel Blob token is configured.
- @trigger.dev/sdk ^4.4.4 installed. trigger.config.ts reads project ref from TRIGGER_PROJECT_REF env var. TRIGGER_SECRET_KEY must be set in .env.local for triggering tasks from server code. Run `npx trigger.dev@latest dev` for local development; deploy with `npx trigger.dev@latest deploy`.
- Prisma 7.8.0 — generated client goes to app/generated/prisma/; import PrismaClient from @/app/generated/prisma/client (no index.ts in v7). Constructor always requires { adapter } argument. @prisma/adapter-pg used for all connections.
- prisma.config.ts uses schema: "prisma/" (multi-file schema) and reads DATABASE_URL from .env via dotenv.
